<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.footballsuretips">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FootballSureTips">
        <activity android:name=".dailytipsrow"></activity>
        <activity android:name=".DailySafeGame" />
        <activity android:name=".DoubleChanceTips" />
        <activity android:name=".JackpotTips" />
        <activity android:name=".GGNGTips" />
        <activity android:name=".PreviousMatches" />
        <activity android:name=".DailyTips" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>